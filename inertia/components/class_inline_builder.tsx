// Retired: class creation now uses ClassForm (single class, no weekday/start time).
// Kept as a thin re-export so any lingering import still resolves.
export { default } from '~/components/class_form'
